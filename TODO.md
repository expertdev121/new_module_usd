- [ ] Update GET function in app/api/manual-donations/[id]/route.ts to await params as Promise
- [ ] Update PUT function in app/api/manual-donations/[id]/route.ts to await params as Promise
- [ ] Update DELETE function in app/api/manual-donations/[id]/route.ts to await params as Promise
- [ ] Run build to verify the type error is resolved
